# 2025년 3회 정보처리기사 필기 기출문제

---

## 제1과목 소프트웨어 설계

## 1. 프로토타이핑 모델(Prototyping Model)에 대한 설명으로 옳지 않은 것은?

① 최종 결과물이 만들어지기 전에 의뢰자가 최종 결과물의 일부를 미리 볼 수 있다.

② 프로토타이핑을 수행하는 과정에서 새로운 요구사항의 발견을 어렵게 한다.

③ 프로토타입은 반드시 사용자와 개발자 모두에게 공통의 참조 모델을 제공한다.

④ 프로토타입은 구현 단계에서 구현 공정이 되는 결과이다.

> **정답: ②**
> 프로토타이핑은 수행 과정에서 새로운 요구사항의 발견을 용이하게 한다.

---

## 2. 자료 흐름도(DFD)의 각 요소별 표기 형태의 연결이 옳지 않은 것은?

① Process : 원

② Data Flow : 화살표

③ Data Store : 삼각형

④ Terminator : 사각형

> **정답: ③**
> Data Store는 삼각형이 아닌 평행선(두 줄)으로 표기한다.

---

## 3. HIPO(Hierarchy Input Process Output)에 대한 설명으로 가장 거리가 먼 것은?

① 상업용 소프트웨어 개발을 위한 문서화 도구이다.

② HIPO 차트 종류에는 가시적 도표, 총체적 도표, 세부적 도표가 있다.

③ 기능과 자료의 의존 관계를 동시에 표현할 수 있다.

④ 보기 쉽고 이해하기 쉽다.

> **정답: ①**
> HIPO는 상업용 소프트웨어 전용 도구가 아닌 시스템 설계 및 문서화를 위한 일반적 도구이다.

---

## 4. 그래픽 표기법을 이용하여 소프트웨어 구성 요소를 모델링하는 객체지향 분석 기법에 포함되지 않는 것은?

① 객체 모델링

② 기능 모델링

③ 동적 모델링

④ 정적 모델링

> **정답: ④**
> 럼바우(Rumbaugh)의 객체지향 분석 3가지 모델링은 객체, 동적, 기능 모델링이다. 정적 모델링은 포함되지 않는다.

---

## 5. 다음 내용이 설명하는 객체지향 설계 원칙은?

- 클라이언트는 사용하지 않는 메소드와 의존관계를 맺으면 안 된다.
- 클라이언트가 사용하지 않는 인터페이스 때문에 영향을 받아서는 안 된다.

① 인터페이스 분리 원칙

② 단일 책임 원칙

③ 개방 폐쇄의 원칙

④ 리스코프 교체의 원칙

> **정답: ①**
> ISP(Interface Segregation Principle): 클라이언트는 자신이 사용하지 않는 인터페이스에 의존하면 안 된다.

---

## 6. N-S(Nassi-Schneiderman) Chart에 대한 설명으로 거리가 먼 것은?

① 논리의 기술에 중점을 둔 도형식 표현 방법이다.

② 연속, 선택 및 다중 선택, 반복 등의 제어 논리 구조로 표현한다.

③ 주로 화살표를 사용하여 논리적인 제어 구조로 흐름을 표현한다.

④ 이해하기 쉽고 코드 변환이 용이하다.

> **정답: ③**
> N-S 차트는 화살표를 사용하지 않고 박스(직사각형) 형태로 표현한다.

---

## 7. 럼바우(Rumbaugh)의 객체지향 분석 절차를 가장 바르게 나열한 것은?

① 객체 모델링 → 동적 모델링 → 기능 모델링

② 객체 모델링 → 기능 모델링 → 동적 모델링

③ 기능 모델링 → 동적 모델링 → 객체 모델링

④ 기능 모델링 → 객체 모델링 → 동적 모델링

> **정답: ①**

---

## 8. 한 모듈 내의 각 구성 요소들이 공통의 목적을 달성하기 위하여 서로 얼마나 관련이 있는지의 기능적 연결 정도를 나타내는 것은?

① Cohesion

② Coupling

③ Structure

④ Unity

> **정답: ①**
> 응집도(Cohesion)는 모듈 내부 요소들의 기능적 연관 정도를 나타낸다.

---

## 9. 결합도(Coupling) 단계를 약한 순서에서 강한 순서로 가장 올바르게 표시한 것은?

① Control → Data → Stamp → Common → Content

② Control → Data → Stamp → Common → Content

③ Content → Stamp → Control → Common → Data

④ Data → Stamp → Control → Common → Content

> **정답: ④**
> 결합도 약 → 강 순서: Data < Stamp < Control < External < Common < Content

---

## 10. UML에서 시퀀스 다이어그램의 구성 항목에 해당하지 않는 것은?

① 생명선

② 실행

③ 확장

④ 메시지

> **정답: ③**
> 시퀀스 다이어그램 구성: 생명선(Lifeline), 실행(Activation), 메시지(Message), 객체(Object) 등. 확장은 유스케이스 다이어그램의 구성요소이다.

---

## 11. 다음 종류의 멀티 터치(Multi-touch), 동작 인식(Gesture Recognition) 등 자연스러운 입력 방식으로 응용을 인식하여 서로 교류하는 정보를 제공하는 사용자 인터페이스를 의미하는 것은?

① GUI(Graphical User Interface)

② OUI(Organic User Interface)

③ NUI(Natural User Interface)

④ CLI(Command Line Interface)

> **정답: ③**

---

## 12. LOC 기법에 의하여 예측된 총 라인수가 36000라인, 기법에 참여할 프로그래머가 6명, 프로그래머의 평균 생산성이 월당 300라인일 때 소요되는 기간을 계산한 결과로 옳은 것은?

① 5개월

② 10개월

③ 15개월

④ 20개월

> **정답: ④**
> 월 생산량 = 6명 × 300라인 = 1800라인/월, 소요 기간 = 36000 / 1800 = 20개월

---

## 13. 분산 시스템에서 미들웨어(Middleware)와 관련한 설명으로 틀린 것은?

① 분산 시스템에서 다양한 부분을 관리하고 통신하며 데이터를 교환해 주는 소프트웨어로 볼 수 있다.

② 위치 투명성(Location Transparency)을 제공한다.

③ 분산 시스템의 여러 컴퓨터들이 요구하는 재사용 가능한 서비스의 구현을 제공한다.

④ 애플리케이션과 사용자 사이에서만 분산 서비스를 제공한다.

> **정답: ④**
> 미들웨어는 애플리케이션-사용자 간뿐만 아니라 이기종 시스템 간 통신과 데이터 교환을 지원한다.

---

## 14. 객체에게 어떤 행위를 하도록 지시하는 명령은?

① Class

② Package

③ Object

④ Message

> **정답: ④**

---

## 15. 에자일 소프트웨어 개발 기법의 가치가 아닌 것은?

① 프로세스와 도구보다는 개인과 상호작용에 더 가치를 둔다.

② 계약 협상보다는 고객과의 협업에 더 가치를 둔다.

③ 실제 작동하는 소프트웨어보다 이해하기 좋은 문서에 더 가치를 둔다.

④ 계획을 따르기보다는 변화에 대응하는 것에 더 가치를 둔다.

> **정답: ③**
> 애자일은 포괄적 문서보다 실제 작동하는 소프트웨어를 더 가치 있게 여긴다.

---

## 16. GoF(Gangs of Four) 디자인 패턴의 구조 패턴에 속하지 않는 것은?

① Composite

② Observer

③ Adapter

④ Decorator

> **정답: ②**
> Observer는 행위(Behavioral) 패턴이다. 구조 패턴: Adapter, Bridge, Composite, Decorator, Facade, Flyweight, Proxy

---

## 17. UML에 대한 설명으로 옳지 않은 것은?

① OMG에서 만든 통합 모델링 언어로서 객체 지향적 분석, 설계 방법론의 표준 지정을 목표로 한다.

② 애플리케이션을 개발할 때 여러 가지 유형의 다이어그램을 제공한다.

③ 실시간 시스템 및 분산 시스템과 같은 시스템의 분석과 설계에는 사용할 수 없다.

④ 개발자와 고객 또는 개발자 상호 간의 의사 소통을 원활하게 할 수 있다.

> **정답: ③**
> UML은 실시간 시스템, 분산 시스템 등 다양한 시스템의 분석과 설계에도 사용할 수 있다.

---

## 18. 미들웨어(Middleware)에 대한 설명으로 틀린 것은?

① 운영체제에서 응용 프로그램들 사이에 위치한 소프트웨어이다.

② 미들웨어의 서비스 이용을 위해 사용자가 정보 고유 방법 등의 내부 동작을 잘 확인할 수 있어야 한다.

③ 소프트웨어 컴포넌트를 이용할 수 있는 인프라 구조를 제공한다.

④ 컴포넌트를 1대 1, 1대 다, 다대 다 등 여러 가지 형태로 연결이 가능하다.

> **정답: ②**
> 미들웨어는 내부 동작을 숨기고 표준화된 인터페이스를 통해 서비스를 제공하는 것이 핵심이다.

---

## 19. 럼바우(Rumbaugh)의 객체지향 분석 기법 중 자료 흐름도(DFD)를 주요 이용하는 것은?

① 기능 모델링

② 동적 모델링

③ 객체 모델링

④ 정적 모델링

> **정답: ①**
> 기능 모델링(Functional Modeling)은 DFD를 사용하여 데이터 흐름을 표현한다.

---

## 20. 모듈화를 통해 분리된 시스템의 각 기능들로, 서브루틴, 서브시스템, 소프트웨어 내의 프로그램, 작업 단위 등과 같은 의미로 사용되는 것은?

① Module

② Component

③ Things

④ Prototype

> **정답: ①**

---

## 제2과목 소프트웨어 개발

## 21. ISO/IEC 9126의 소프트웨어 품질 특성 중 기능성(Functionality)의 하위 특성으로 옳지 않은 것은?

① 학습성

② 적합성

③ 정확성

④ 보안성

> **정답: ①**
> 학습성(Learnability)은 기능성이 아닌 사용성(Usability)의 하위 특성이다.

---

## 22. SW 패키징 도구 활용 시 고려사항과 거리가 먼 것은?

① 패키징 시 사용자를 고려하여 패키징 시 보안을 고려한다.

② 사용자 편의성을 위한 복잡한 기능과 비효율성 문제를 고려한다.

③ 사용성 단일 기기에서만 사용할 수 있도록 해야 한다.

④ 제품 SW종류에 적합한 암호화 알고리즘을 적용한다.

> **정답: ③**
> SW 패키징은 다양한 기기에서 사용 가능하도록 고려해야 한다.

---

## 23. 하향식 통합에 있어서 모듈 간의 통합 시험을 위해 일시적으로 필요한 조건들을 가지고 임시로 제공되는 시험용 모듈을 무엇이라고 하는가?

① Stub

② Driver

③ Procedure

④ Function

> **정답: ①**
> Stub: 하향식 통합 시 하위 모듈 대신 사용하는 더미 모듈. Driver: 상향식 통합 시 상위 모듈 대신 사용하는 더미 모듈.

---

## 24. 소프트웨어 재공학이 소프트웨어의 재개발에 비해 갖는 장점으로 옳은 것은?

① 위험부담 감소

② 비용 절감

③ 시스템 명령의 오류억제

④ 개발 기간 단축

> **정답: ④**

---

## 25. 제품 소프트웨어의 형상 관리 역할로 틀린 것은?

① 형상 관리를 통해 이전 리비전이나 버전에 대한 정보에 접근 기능하여 여러 곳에 관리에 유용

② 불필요한 사용자의 소스 수정 제한

③ 프로젝트 개발비용을 효율적으로 관리

④ 동일한 프로젝트에 대해 여러 개발자가 동시 개발 가능

> **정답: ③**
> 형상 관리는 비용 관리 도구가 아닌 변경 사항 관리 및 버전 통제 도구이다.

---

## 26. 제어흐름 그래프가 다음과 같을 때 McCabe's cyclomatic 수는 얼마인가?

```
    a
   / \
  b   d
 / \
c   (b→d)
 \
  (c→d)
```

① 3

② 4

③ 5

④ 6

> **정답: ②**
> V(G) = E - N + 2 = 6 - 4 + 2 = 4

---

## 27. 디지털 저작권 관리(DRM) 기능과 거리가 먼 것은?

① 콘텐츠 암호화 및 키 관리

② 콘텐츠 식별체계 표현

③ 콘텐츠 오류 감지 및 복구

④ 라이선스 발급 및 관리

> **정답: ③**
> 콘텐츠 오류 감지 및 복구는 DRM의 기능이 아닌 오류 복구(Error Recovery) 기능에 해당한다.

---

## 28. 다음이 설명하는 테스트 용어는?

> 테스트의 결과가 참인지 거짓인지를 판단하기 위해서 사전에 정의된 참값을 입력하여 비교하는 기준 및 활동을 말한다. 종류에는 참, 샘플링, 휴리스틱, 일관성 검사가 존재한다.

① 테스트 케이스

② 테스트 시나리오

③ 테스트 오라클

④ 테스트 데이터

> **정답: ③**

---

## 29. 다음 트리에 대한 INORDER 운행 결과는?

```
      A
     / \
    B   C
   /   / \
  D   E   F
```

① D B A E C F

② A B D C E F

③ D B E C F A

④ A B C D E F

> **정답: ①**
> INORDER(중위 순회): Left → Root → Right → D, B, A, E, C, F

---

## 30. 검증(Validation) 검사 기법 중 개발자의 장소에서 사용자가 개발자 앞에서 행해지며, 오류와 비상의 문제점을 사용자와 개발자가 함께 확인하면서 실시하는 검사는?

① 디버깅 검사

② 형상 검사

③ 자료구조 검사

④ 알파 검사

> **정답: ④**
> 알파 검사(Alpha Test): 개발자 환경에서 사용자가 직접 테스트하며 개발자가 관찰하는 방식

---

## 31. 해싱 함수(Hashing Function)의 종류가 아닌 것은?

① 제곱법(Mid-Square)

② 숫자 분석법(Digit Analysis)

③ 개방주소법(Open Addressing)

④ 제산법(Division)

> **정답: ③**
> 개방주소법(Open Addressing)은 해싱의 충돌(Collision) 해결 방법이며, 해싱 함수의 종류가 아니다.

---

## 32. 퀵 정렬에 관한 설명으로 옳은 것은?

① 레코드의 키 값을 분석하여 같은 값끼리 그 순서에 맞는 버킷에 분배하였다가 버킷의 순서에 따라 레코드를 꺼내어 정렬한다.

② 주어진 파일에서 인접한 두 개의 레코드 키 값을 비교하여 그 크기에 따라 레코드 위치를 서로 교환한다.

③ 레코드의 많은 자료 이동을 없애고 하나의 파일을 부분적으로 나누어 가면서 정렬하는 방법이다.

④ 입력 파일이 어떤 상태이더라도 일정한 탐색 시간을 보장한다.

> **정답: ③**
> ①은 버킷 정렬, ②는 버블 정렬에 대한 설명이다.

---

## 33. 소프트웨어 설치 매뉴얼에 포함될 항목이 아닌 것은?

① 제품 소프트웨어 개요

② 설치 관련 파일

③ 폴더 및 설치 경로

④ 소프트웨어 개발 기간

> **정답: ④**

---

## 34. 다음 단위 테스트로 발견할 수 있는 오류가 아닌 것은?

① 알고리즘 오류에 따른 잘못된 결과

② 탈출구가 없는 반복문의 존재

③ 모듈의 비정상적인 상호 작용으로 인한 원치 않는 결과

④ 틀린 연산 우선순위로 인한 잘못된 결과

> **정답: ③**
> 모듈 간 상호 작용 오류는 단위 테스트가 아닌 통합 테스트에서 발견한다.

---

## 35. 다음 설명의 소프트웨어 버전 관리 도구 방식은?

> - 버전 관리 자료가 원격 저장소와 로컬 저장소에 함께 저장되어 관리된다.
> - 로컬관리가 가능하여 원격 저장소에 문제가 생겨도 로컬 저장소의 자료를 이용하여 작업할 수 있다.
> - 대표적인 버전 관리 도구로 Git이 있다.

① 단일 장소 보관 방식

② 분산 저장소 방식

③ 공유 폴더 방식

④ 클라이언트-서버 방식

> **정답: ②**

---

## 36. 다음 중 최악의 경우 탐색 검색 시간이 가장 느린 트리 구조는?

① 이진 탐색 트리

② AVL 트리

③ 2-3 트리

④ 레드-블랙 트리

> **정답: ①**
> 이진 탐색 트리(BST)는 최악의 경우(편향 트리) O(n)이다. 나머지는 균형 트리로 O(log n)을 보장한다.

---

## 37. 코드의 간결성을 유지하기 위해 사용하는 지침으로 틀린 것은?

① 공백을 이용하여 설명 그룹과 구분을 명확히 구분한다.

② 복잡한 논리식과 산술식은 걷어내기(Indentation)를 통해 명확히 표현한다.

③ 빈 줄을 사용하여 선언부와 구현부를 구분한다.

④ 한 줄에 최대한 많은 문장을 코드화한다.

> **정답: ④**
> 한 줄에 여러 문장을 넣으면 가독성이 떨어져 코드 간결성에 반한다.

---

## 38. 다음 중 선형 구조로만 묶은 것은?

① 스택, 트리

② 큐, 데크

③ 큐, 그래프

④ 리스트, 그래프

> **정답: ②**
> 선형 구조: 스택, 큐, 데크, 리스트 / 비선형 구조: 트리, 그래프

---

## 39. 코드 인스펙션과 관련한 설명으로 틀린 것은?

① 프로그램을 실행시키는 것 없이 눈으로 확인하는 방법으로 볼 수 있다.

② 코드 품질 향상 기법 중 하나이다.

③ 인스펙션 시에만 활용되는 기법이다.

④ 결함과 함께 코드 표준 준수 여부, 효율성 등 다른 품질 이슈를 동시에 검사한다.

> **정답: ③**
> 코드 인스펙션은 다양한 개발 단계에서 활용되는 정적 검사 기법이다.

---

## 40. 소프트웨어를 재사용함으로써 얻을 수 있는 이점으로 가장 거리가 먼 것은?

① 생산성 증가

② 프로젝트 문서 공유

③ 소프트웨어 품질 향상

④ 새로운 개발 방법론 도입 용이

> **정답: ④**
> 소프트웨어 재사용은 검증된 모듈을 활용하는 것으로, 새로운 개발 방법론 도입과는 직접적 관련이 없다.

---

## 제3과목 데이터베이스 구축

## 41. 다음 설명의 ( ) 안에 들어갈 내용으로 적절한 것은?

> 후보키를 구성하면 있는 모든 튜플에 대해 유일성과 ( )을 모두 만족시켜야 한다.

① 중복성

② 최소성

③ 참조성

④ 동일성

> **정답: ②**
> 후보키(Candidate Key)의 조건: 유일성(Uniqueness) + 최소성(Minimality)

---

## 42. E-R 모델의 표현 방법으로 옳지 않은 것은?

① 관계 : 마름모

② 속성 : 타원형

③ 개체 : 삼각형

④ 연결 : 선

> **정답: ③**
> 개체(Entity)는 삼각형이 아닌 직사각형으로 표기한다.

---

## 43. 다음 데이터베이스 무결성 제약 중 '개체 무결성 제약' 조건에 대한 설명으로 맞는 것은?

① 데이터 내의 특성들이 각 속성이 정의된 소속 도메인에 지정된 값을 가져야 한다.

② 릴레이션의 기본키를 구성하는 어떤 속성값도 널 값이나 중복값을 가질 수 없다.

③ 릴레이션은 참조할 수 없는 외래키 값을 가질 수 없다.

④ 릴레이션의 기본키를 구성하는 속성값은 중복값을 가질 수 있지만 널 값을 가질 수 없다.

> **정답: ②**
> 개체 무결성: 기본키는 NULL과 중복값 모두 불가. ①=도메인 무결성, ③=참조 무결성, ④=일부만 맞음(중복도 불가).

---

## 44. 릴레이션에 대한 설명으로 거리가 먼 것은?

① 튜플들의 삽입, 삭제 등의 작업으로 인해 릴레이션은 시간에 따라 변한다.

② 한 릴레이션에 포함된 튜플들은 모두 상이하다.

③ 애트리뷰트는 논리적으로 더 이상 쪼갤 수 없는 원자값으로 구성된다.

④ 한 릴레이션에 포함된 튜플 사이에는 순서가 있어야 한다.

> **정답: ④**
> 관계형 데이터베이스에서 튜플 간의 순서는 의미가 없다(순서 없음).

---

## 45. player 테이블에 player_name, team_id, height 컬럼이 존재한다. 아래 SQL문에서 문법적 오류가 있는 부분은?

```sql
(1) SELECT player_name, height
(2) FROM player
(3) WHERE team_id = 'korea'
(4) AND height BETWEEN 170 OR 180;
```

① (1)

② (2)

③ (3)

④ (4)

> **정답: ④**
> BETWEEN 절의 올바른 문법: `BETWEEN 170 AND 180` (OR가 아닌 AND 사용)

---

## 46. DCL(Data Control Language) 명령어가 아닌 것은?

① COMMIT

② ROLLBACK

③ GRANT

④ SELECT

> **정답: ④**
> SELECT는 DML(Data Manipulation Language)이다. DCL: GRANT, REVOKE / TCL: COMMIT, ROLLBACK

---

## 47. 릴레이션 R의 모든 결정자(Determinant)가 후보키이면 그 릴레이션 R은 어떤 정규형에 속하는가?

① 제 1 정규형

② 제 2 정규형

③ 보이스/코드 정규형

④ 제 4 정규형

> **정답: ③**
> BCNF(Boyce-Codd Normal Form): 모든 결정자가 후보키인 정규형

---

## 48. 릴레이션 A1의 도메인이 3개이고, A2의 도메인이 2개이고, A3의 도메인이 4개 갖는다. 이 릴레이션들이 표현할 수 있는 최대 튜플(Tuple)의 최대 수는?

① 24

② 12

③ 8

④ 6

> **정답: ①**
> 최대 튜플 수 = 3 × 2 × 4 = 24

---

## 49. 관계대수에 대한 설명으로 틀린 것은?

① 주어진 릴레이션 조작을 위한 언어의 집합이다.

② 일반 집합 연산과 순수 관계 연산으로 구분된다.

③ 대상 해를 구하는 방법을 명시하는 절차적 정형 언어이다.

④ 원하는 정보와 그 정보를 어떻게 유도하는가를 기술하는 비절차적 방법이다.

> **정답: ④**
> 관계대수는 절차적(Procedural) 언어이다. 비절차적 언어는 관계해석(Relational Calculus)이다.

---

## 50. 정규화, 속성, 관계를 시스템의 형성과 개발 운영의 단순화를 위해 중복, 통합, 분리 등을 수행하는 데이터 모델링 기법은?

① 정규화

② 반정규화

③ 단편화

④ 이행

> **정답: ②**
> 반정규화(Denormalization): 성능 최적화를 위해 중복, 통합, 분리 등을 수행하는 기법

---

## 51. 트랜잭션을 수행하는 도중 장애로 인해 손상된 데이터베이스를 손상되기 이전의 정상적인 상태로 복구시키는 작업은?

① Recovery

② Commit

③ Abort

④ Restart

> **정답: ①**

---

## 52. 시스템 카탈로그에 대한 설명으로 틀린 것은?

① 시스템 카탈로그의 갱신은 무결성 유지를 위해 SQL을 이용한 사용자가 직접 갱신하여야 한다.

② 데이터베이스에 저장되는 정보는 데이터베이스에 대한 정보를 유지관리한다.

③ DBMS가 스스로 생성하고 유지하는 데이터베이스 내의 특별한 테이블의 집합이다.

④ 데이터 사전이라고도 한다.

> **정답: ①**
> 시스템 카탈로그는 DBMS가 자동으로 생성·관리하며 사용자가 직접 수정할 수 없다.

---

## 53. 속성(attribute)에 대한 설명으로 틀린 것은?

① 속성은 개체의 특성을 기술한다.

② 속성의 수를 차수(degree)라고 한다.

③ 속성은 데이터베이스를 구성하는 가장 작은 논리적 단위이다.

④ 속성 수를 카디널리티(Cardinality)라고 한다.

> **정답: ④**
> 카디널리티(Cardinality)는 튜플의 수이고, 속성의 수는 차수(Degree)이다.

---

## 54. 다음 정규화 2차 이상의 이상 현상(Anomaly)의 종류에 해당하지 않는 것은?

① 검색 이상

② 삽입 이상

③ 삭제 이상

④ 갱신 이상

> **정답: ①**
> 이상 현상(Anomaly)의 종류: 삽입 이상, 삭제 이상, 갱신(수정) 이상. '검색 이상'은 존재하지 않는다.

---

## 55. 다음 관계 대수 중 순수 관계 연산자가 아닌 것은?

① 차집합(Difference)

② 프로젝션

③ 조인(Join)

④ 디비전(Division)

> **정답: ①**
> 차집합(Difference)은 일반 집합 연산자이다. 순수 관계 연산자: Select, Project, Join, Division

---

## 56. 트랜잭션의 주요 특성 중 하나로, 둘 이상의 트랜잭션이 동시에 병행되는 경우에는 어느 하나의 트랜잭션도 다른 트랜잭션의 연산에 끼어들 수 없음을 의미하는 것은?

① Log

② Transparency

③ Isolation

④ Durability

> **정답: ③**
> Isolation(고립성): 트랜잭션 실행 중 다른 트랜잭션의 간섭 불가

---

## 57. 관계형 데이터베이스에서 다음 설명에 해당하는 키(Key)는?

> 한 릴레이션 내의 속성들의 집합으로 구성된 키로서, 릴레이션을 구성하는 모든 튜플에 대해 유일성은 만족시키지만 최소성은 만족시키지 못한다.

① 후보키

② 대체키

③ 슈퍼키

④ 외래키

> **정답: ③**
> 슈퍼키(Super Key): 유일성 O, 최소성 X / 후보키(Candidate Key): 유일성 O, 최소성 O

---

## 58. 물리적 데이터베이스 설계에 대한 설명으로 거리가 먼 것은?

① 물리적 데이터베이스 설계는 논리적 설계를 유효한 방법으로 변환하는 것이다.

② 저장 레코드의 형식, 순서, 접근 경로와 같은 정보를 사용하여 설계한다.

③ 트랜잭션 처리량과 응답시간, 디스크 용량 등을 고려해야 한다.

④ 트랜잭션의 일식, 순서, 접근 경로와 관계없이 데이터 타입만을 단일 관계로 표현한다.

> **정답: ④**
> 물리적 설계는 접근 경로, 저장 구조 등을 포함하여 실제 저장 방식을 결정한다.

---

## 59. 뷰(View)에 대한 설명으로 틀린 것은?

① 뷰 위에 또 다른 뷰를 정의할 수 있다.

② 뷰에 대한 검색은 기본 테이블과 거의 동일하게 사용할 수 있다.

③ 사용자가 필요한 정보를 요구에 맞게 가공하여 뷰로 만들 수 있다.

④ SQL을 사용하여 뷰에 대한 삽입, 갱신, 삭제 연산 시 제약 사항이 없다.

> **정답: ④**
> 뷰에 대한 삽입·갱신·삭제에는 여러 제약 사항이 존재한다(예: 집계 함수 포함 뷰는 갱신 불가).

---

## 60. 데이터 모델의 구성 요소 중 데이터 구조에 따라 개념 세계나 컴퓨터 세계에서 실제로 표현된 값들을 처리하는 작업을 의미하는 것은?

① Data Structure

② Operation

③ Relation

④ Constraint

> **정답: ②**
> 데이터 모델 3요소: 구조(Structure), 연산(Operation), 제약(Constraint)

---

## 제4과목 프로그래밍 언어 활용

## 61. C 언어에서 산술 연산자가 아닌 것은?

① %

② *

③ <<

④ /

> **정답: ③**
> `<<`는 비트 왼쪽 시프트 연산자(비트 연산자)이다. 산술 연산자: +, -, *, /, %

---

## 62. 다음 C 언어 프로그램의 결과로 옳은 것은?

```c
#include <stdio.h>
main() {
    int i = 0;
    while (1) {
        if (i == 4)
            break;
        i++;
    }
    printf("%d", i);
}
```

① 3

② 4

③ 5

④ 6

> **정답: ②**
> i가 4가 되면 break로 루프 종료. printf("%d", i) → 4 출력

---

## 63. 다음 중 IP 버전에 대한 설명 중 틀린 것은?

① IPv4는 각 부분을 숫자로 구성하고, 총 32비트로 구성된다.

② IPv6는 각 부분을 콜론으로 구분한다.

③ 네트워크 부분이 길이에 따라 IP 주소 A 클래스에서 E 클래스까지 총 5단계로 구성되어 있다.

④ IPv6는 IPv4에 비해 자료 전송 속도가 느리다.

> **정답: ④**
> IPv6는 IPv4보다 더 빠른 전송 속도를 제공하며, 확장성과 보안성도 더 우수하다.

---

## 64. 정수를 입력받아 처리하는 다음 C 언어 프로그램에서 괄호에 들어갈 알맞은 코드는?

```c
#include <stdio.h>
int main(void) {
    int n, sum = 3;
    {         }
    sum = sum + n;
    printf("%d", sum);
}
```

① scanf("%d", n);

② scanf("%d", &n);

③ scanf("%f", n);

④ scanf("%f", &n);

> **정답: ②**
> 정수형 입력은 `%d`, 변수 주소 전달을 위해 `&n` 사용

---

## 65. 다음은 n각형 화면에 그리는 프로그램을 Python으로 구현한 것이다. 괄호(③~⑤)에 들어갈 알맞은 코드는?

```python
import turtle
( ③ ) shape(distance, n):
    t = turtle.Turtle()
    for i in range(n):
        t.( ④ )
        t.left( ⑤ )
shape(100, 5)
```

① def, forward(distance), left(360//n)

② def, forward(distance), left(360///n)

③ class, forward(distance), left(360//n)

④ class, forward(distance), left(360///n)

> **정답: ①**
> 함수 정의는 `def`, 이동은 `forward(distance)`, 회전 각도는 `360//n` (정수 나눗셈)

---

## 66. 다음 JAVA 프로그램이 실행되었을 때의 결과는?

```java
public class ovr {
    public static void main(String[] args) {
        int a = 1, b = 2, c = 3, d = 4;
        int mx, mn;
        mx = a < b ? a : b;
        if (mx == 1) {
            mn = a > mx ? b : c;
        }
        else {
            mn = b < mx ? c : d;
        }
        System.out.println(mn);
    }
}
```

① 1

② 2

③ 3

④ 4

> **정답: ③**
> mx = (1<2) ? 1 : 2 = 1 → if(mx==1) TRUE → mn = (1>1) ? 2 : 3 = 3 → 출력 3

---

## 67. 개발 환경 구성을 위한 빌드(Build) 도구에 해당하지 않는 것은?

① Ant

② Kerberos

③ Maven

④ Gradle

> **정답: ②**
> Kerberos는 네트워크 인증 프로토콜이다. 빌드 도구: Ant, Maven, Gradle, Make 등

---

## 68. 프로세스와 관련한 설명으로 틀린 것은?

① 프로세스가 대기 상태에서 프로세서가 배당되어 실행 상태로 변화하는 것을 디스패치(Dispatch)라 한다.

② 프로세스 제어 블록(PCB, Process Control Block)은 프로세스 식별자, 프로세스 상태 등의 정보를 구성한다.

③ 이전 프로세스의 실행 레지스터 내용을 보관하고 다른 프로세스의 레지스터를 적재하는 것을 문맥 교환(Context Switching)이라고 한다.

④ 프로세스는 스레드(Thread) 내에서 실행되는 흐름의 단위이며, 스레드와 달리 주소 공간에 실행 스택(Stack)이 없다.

> **정답: ①**
> 디스패치(Dispatch)는 대기(Blocked) 상태가 아닌 준비(Ready) 상태에서 실행(Running) 상태로의 전환이다.

---

## 69. 순차 파일에 대한 설명으로 옳지 않은 것은?

① 연속적인 순서로 처리하므로 처리 속도가 다른 파일보다 빠르다.

② 파일을 레코드로 구성할 때 각 기억 공간에 빈 공간이 존재하여 효율적으로 이용할 수 없다.

③ 연속적인 공간이므로 추가, 삽입, 삭제, 수정이 어렵다.

④ 대용량 데이터의 일괄 처리(Batch Processing)에 적합하다.

> **정답: ②**
> 순차 파일은 레코드가 연속 저장되어 빈 공간이 없으므로 기억 공간을 효율적으로 사용한다.

---

## 70. C 언어에서 변수로 사용할 수 없는 것은?

① data02

② int01

③ _sub

④ short

> **정답: ③**
> 언더스코어(_)로 시작하는 식별자는 C 표준에서 시스템 및 구현체 내부용으로 예약되어 있어 사용자 변수로 사용할 수 없다.

---

## 71. 다음 중 가장 약한 결합도(Coupling)는?

① Common Coupling

② Content Coupling

③ External Coupling

④ Stamp Coupling

> **정답: ④**
> 결합도 강도 순: Content > Common > External > Control > Stamp > Data. 보기 중 Stamp가 가장 약하다.

---

## 72. 다음과 같은 형태로 임계 구역의 접근을 제어하는 상호배제 기법은?

```
P(S) : while S < 0 do skip;
S := S - 1;
V(S) : S := S + 1;
```

① Dekker Algorithm

② Lamport Algorithm

③ Peterson Algorithm

④ Semaphore

> **정답: ④**
> P(S)/V(S) 연산은 세마포어(Semaphore)의 대기(wait)와 신호(signal) 연산이다.

---

## 73. 다음 파이썬 코드에서 '53t44'를 입력했을 때 출력 결과는?

```python
a, b = map(int, input().split("t"))
print(a, b)
```

① 53 | 44

② 53t44

③ 53 44

④ 53, 44

> **정답: ③**
> '53t44'.split('t') → ['53', '44'] → a=53, b=44 → print(53, 44) → "53 44"

---

## 74. 네트워크 장비에 대한 설명으로 옳지 않은 것은?

① 브라우저는 전송되는 신호의 장거리 전송을 위해 신호를 재생하여 다시 송신하는 기능을 수행한다.

② 브리지는 LAN을 연결하거나 LAN 안에서의 컴퓨터 그룹을 연결하는 기능을 수행하며, 데이터 링크 계층 중 MAC 기반 경로를 사용한다.

③ 스위치는 LAN과 LAN의 연결이 돼서 큰 LAN을 만드는 장치로, OSI 2계층에서 동작한다.

④ 라우터는 LAN과 LAN의 연결 기능이 다른 LAN이나 WAN의 연결도 수행하고, OSI 7계층의 네트워크 계층에서 동작한다.

> **정답: ①**
> 신호를 재생하여 재전송하는 장비는 브라우저(Browser)가 아닌 리피터(Repeater)이다.

---

## 75. HTTP의 잘 알려진(Well Known) 포트 번호는?

① 23

② 80

③ 53

④ 443

> **정답: ②**
> HTTP: 80 / HTTPS: 443 / Telnet: 23 / DNS: 53

---

## 76. 내부 프로그램의 필수 요소에 대한 설명으로 틀린 것은?

① 자동화는 심각한 오류 상황 외에는 사용자의 개입 없이 동작해야 한다.

② 안정성은 어떤 문제가 생겼을 때, 언제 발생했는지 등을 추적할 수 있어야 한다.

③ 로그는 대상 애플리케이션의 처리 내용이나 예외 상황 등을 파악할 수 있도록 기록되어야 한다.

④ 성능은 주어진 시간 내에 처리를 완료할 수 없어도 되고, 동작하고 있는 다른 애플리케이션을 방해하지 않아야 한다.

> **정답: ④**
> 성능은 반드시 주어진 시간 내에 처리를 완료할 수 있어야 한다.

---

## 제5과목 정보시스템 구축 관리

## 77. 빈 기억공간의 크기가 20K, 16K, 8K, 40K의 네 기억장치 배치 전략으로 "Worst Fit"을 사용하여 17K의 프로그램을 적재할 경우 내부 단편화의 크기는?

① 3K

② 23K

③ 44K

④ 67K

> **정답: ②**
> Worst Fit: 가장 큰 공간(40K)에 배치 → 40K - 17K = 23K 남음 (내부 단편화)

---

## 78. IPv4와 IPv6의 주소 전환에 사용되는 기술이 아닌 것은?

① 듀얼 스택

② 터널링

③ 헤더 변환

④ 라우팅

> **정답: ④**
> 라우팅은 IPv4/IPv6 전환 기술이 아닌 일반 네트워크 경로 설정 기술이다. 전환 기술: 듀얼 스택, 터널링, 헤더 변환

---

## 79. JAVA에서 변수와 자료형에 대한 설명으로 틀린 것은?

① 변수는 어떤 값을 저장하기 위한 메모리 공간으로 사용하는 공간이다.

② char 자료형은 한 개의 문자를 저장하고자 할 때 사용한다.

③ 실수 자료형에는 float, short, byte 등이 있다.

④ boolean 자료형은 조건이 참인지 거짓인지 판별할 때 사용한다.

> **정답: ③**
> short, byte는 정수형 자료형이다. 실수 자료형: float, double

---

## 80. 다음 HRN에 대한 설명으로 옳지 않은 것은?

① 대기 시간과 서비스 시간을 고려한다.

② 대기 시간이 긴 프로세스일 경우 우선순위가 높다.

③ 대기 시간이 길어질수록 우선순위가 낮아진다.

④ SJF 기법을 보완하기 위한 스케줄링 방법이다.

> **정답: ③**
> HRN(Highest Response Ratio Next)은 대기 시간이 길수록 우선순위가 높아져 기아 현상을 방지한다.

---

## 81. 다음 내용이 설명하는 기술로 가장 적절한 것은?

> - 통신의 중복에 의해 어디서도 항상 직접 접속되는 그물 모양의 네트워크이다.
> - 비교적 많은 비교적 국가 사이에 구성될 경우 경제적인 관련하지만, 다수의 국 사이에는 회선이 새분히 이용 네트워크로 불편하다.
> - 해당 지역의 무선 네트워크의 경우 최대한 안전하게 전달할 수 있으며 행사장이나 군 등에서 많이 활용된다.

① Virtual Local Area Network

② Simple Station Network

③ Mesh Network

④ Modem Network

> **정답: ③**

---

## 82. DoS(Denial of Service) 공격에 대한 설명으로 틀린 것은?

① DoS 공격은 정상 크기보다 큰 ICMP 패킷을 작은 조각(Fragment)으로 쪼개어 공격 대상이 조각화된 패킷을 받게 하는 방법이다.

② Smurf 공격은 멀티캐스트(Multicast)를 활용하여 공격 대상이 브로드캐스트에 의해 증폭된 응답 패킷을 받게 하는 방법이다.

③ SYN Flooding은 존재하지 않는 클라이언트가 서버별로 한정된 접속 가능 공간에 접속한 것처럼 속여 다른 사용자가 서비스를 이용하지 못하게 하는 방법이다.

④ Land 공격은 패킷 전송 시 발신인 IP 주소와 목적지 IP 주소 값을 모두 공격 대상의 IP 주소로 설정하여 전송하는 방법이다.

> **정답: ②**
> Smurf 공격은 멀티캐스트가 아닌 브로드캐스트를 이용한다.

---

## 83. 비대칭 암호화 방식으로 소수를 활용한 암호화 알고리즘은?

① DES

② AES

③ SMT

④ RSA

> **정답: ④**
> RSA: 두 소수의 곱을 이용하는 비대칭 공개키 암호화 알고리즘

---

## 84. 다음 내용이 설명하는 것은?

> - 네트워크상에 광채널 스위치에 이점인 고속 전송과 장거리 연결 및 연결된 여러 서버들의 스토리지를 각기 다른 운영체제를 가진 여러 기종이 네트워크상에서 동일 및 집적회로 데이터를 공유할 수 있도록 하며, 여러 개의 저장장치나 백업 장비를 단일화시킨 시스템

① SAN

② MBR

③ NAC

④ NIC

> **정답: ①**
> SAN(Storage Area Network): 여러 서버와 스토리지를 고속 네트워크로 연결한 스토리지 전용 네트워크

---

## 85. 정보 보안의 침입통제 정책 중에 해당하지 않는 것은?

① 물리적 접근 통제

② 데이터 전달 접근 통제

③ 논리적 접근 통제

④ 역할 기반 접근 통제

> **정답: ②**
> 침입통제 정책: 물리적 접근 통제, 논리적 접근 통제, 역할 기반 접근 통제 등. '데이터 전달 접근 통제'는 일반적인 침입통제 분류에 해당하지 않는다.

---

## 86. 다음이 설명하는 것은?

> - 사물통신, 사물인터넷과 같이 대역폭이 제한된 통신에 최적화되어 개발된 푸시기술 기반의 경량 메시지 전송 프로토콜
> - 메시지 브로커(Broker)를 통해 송신자가 특정 메시지를 발행하거나, 수신자가 특정 메시지를 구독하는 방식
> - IBM이 주도하여 개발

① GRID

② TELNET

③ GPN

④ MQTT

> **정답: ④**

---

## 87. 다음이 설명하는 용어로 옳은 것은?

> - 오픈 소스를 기반으로 한 분산 컴퓨팅 플랫폼이다.
> - 일반 PC급 컴퓨터들로 가상화하여 대형 스토리지를 형성한다.
> - 다양한 소스를 통해 생성된 빅데이터를 효율적으로 저장하고 처리한다.

① 하둡(Hadoop)

② 비콘(Beacon)

③ 포스퀘어(Foursquare)

④ 멤리스터(Memristor)

> **정답: ①**

---

## 88. COCOMO 모델에 의한 비용 산정에 대한 설명으로 옳지 않은 것은?

① 프로젝트 유형에 따라 구분에 따라 세분화된 프로젝트의 비용 유형 모델이다.

② 프로젝트의 규모의 소프트웨어에 그 유형에 따라 비용이 다르게 산정된다.

③ 비용 산정 유형으로 Organic Mode, Embedded Mode, Semi-Detached Mode가 있다.

④ UFP(Unadjusted Function Point)를 계산한다.

> **정답: ④**
> COCOMO는 LOC(코드 라인 수) 기반 비용 산정 모델이다. UFP는 기능점수(FP) 방법에서 사용한다.

---

## 89. 세션 하이재킹을 탐지하는 방법으로 거리가 먼 것은?

① FTP SYN SEGMENT 탐지

② 비동기식 상태 탐지

③ ACK STORM 탐지

④ 패킷의 유실 및 재전송 증가 탐지

> **정답: ①**
> 세션 하이재킹 탐지: 비동기 상태 탐지, ACK STORM 탐지, 패킷 유실·재전송 증가 탐지 등. FTP SYN SEGMENT 탐지는 해당 없음.

---

## 90. 다음 중 프로토타입 모형을 선택하는 것이 가장 적합한 경우는?

① 구축하고자 하는 시스템의 요구사항이 불분명할 때

② 시간의 제약으로 시스템만을 보기 위한 경우

③ 고객이 개발 과정에 참여하지 않을 때

④ 시스템 전반에 걸쳐 발생할 수 있는 위험을 최소화하고자 할 때

> **정답: ①**
> 프로토타입 모형: 요구사항이 불명확할 때 시제품을 만들어 요구사항을 명확히 하는 모형

---

## 91. 인증의 유형 중에서 패스워드를 사용하는 경우에 해당하는 인증 유형은?

① Something You Have

② Something You Are

③ Something You Know

④ Somewhere You Are

> **정답: ③**
> 패스워드, PIN 등 지식 기반 인증 = Something You Know

---

## 92. 취약점 관리를 위한 응용 프로그램의 보안 설정과 가장 거리가 먼 것은?

① 서버 파라미터 출입 통제

② 실행 프로세스 권한 설정

③ 운영체제의 접근 제한

④ 운영체제의 정보 수집 제한

> **정답: ①**
> 서버 파라미터 출입 통제는 응용 프로그램 보안 설정 범주에 해당하지 않는다.

---

## 93. 다음에 제시된 프로토콜과 포트의 연결이 잘못된 것은?

① UTP 53 포트 - SNMP

② TCP 23 포트 - Telnet

③ UDP 69 포트 - TFTP

④ UTP 111 포트 - RFC

> **정답: ①**
> 포트 53 = DNS, SNMP = 포트 161(UDP). ② Telnet:23 ③ TFTP:69 는 정확하다.

---

## 94. 프로젝트에 내재된 위험 요소를 인식하고 그 영향을 분석하여 이를 관리하는 활동으로서, 프로젝트를 성공시키기 위해 위험 요소를 사전에 예측, 대비하는 모든 기술과 활동을 포함하는 것은?

① Critical Path Method

② Risk Analysis

③ Work Breakdown Structure

④ Waterfall Model

> **정답: ②**

---

## 95. 입력 데이터 검증 및 표현과 관련된 설명으로 옳지 않은 것은?

① SQL 삽입: 웹 응용 프로그램에 SQL을 삽입하여 내부 데이터베이스(DB) 서버의 데이터를 유출 및 변조하고, 관리자 인증을 우회한다.

② 크로스사이트 스크립트: 웹페이지에 악의적인 스크립트를 삽입하여 방문자들의 정보를 탈취한다.

③ 자원 삽입: 악의적인 명령이 포함된 스크립트 파일을 업로드함으로써 시스템에 손상을 준다.

④ 운영체제 명령어 삽입: 외부 입력값을 통해 시스템 명령어의 실행을 유도함으로써 권한을 탈취하거나 시스템 장애를 유발한다.

> **정답: ③**
> ③은 파일 업로드 취약점(Unrestricted File Upload)에 대한 설명이다. 자원 삽입은 사용 가능한 자원(파일, 포트 등)을 악의적으로 지정하는 공격이다.

---

## 96. 어떤 외부 컴퓨터가 접속되면 접속 인가 여부를 점검해서 인가된 경우에는 접속이 허용되고, 그 반대의 경우에는 거부할 수 있는 접근제어 유틸리티는?

① tcp wrapper

② trace checker

③ token finder

④ change detector

> **정답: ①**
> tcp_wrapper: 네트워크 서비스에 대한 접근을 호스트 기반으로 제어하는 유틸리티

---

## 97. 위조된 매체 접근 제어(MAC) 주소를 지속적으로 네트워크로 흘려보내, 스위치 MAC 주소 테이블의 저장 기능을 혼란시켜 더미 허브(Dummy Hub)처럼 작동하게 하는 공격은?

① Parsing

② LAN Tapping

③ Switch Jamming

④ FTP Flooding

> **정답: ③**

---

## 98. 국내 IT 서비스 경쟁력 강화를 목표로 개발되었으며 인프라 제어 및 관리 환경, 실행 환경, 개발 환경, 서비스 환경, 운영 환경으로 구성되어 있는 개방형 클라우드 컴퓨팅 플랫폼은?

① N2OS

② PaaS-TA

③ KAWS

④ Metaverse

> **정답: ②**
> PaaS-TA: 국내 개발 개방형 클라우드 플랫폼

---

## 99. Secure OS의 보안 기능으로 거리가 먼 것은?

① 식별 및 인증

② 임의적 접근 통제

③ 고가용성 지원

④ 강제적 접근 통제

> **정답: ③**
> 고가용성(High Availability) 지원은 보안 기능이 아닌 가용성(Availability) 기능이다.

---

## 100. 다음 암호 알고리즘 중 성격이 다른 하나는?

① MD4

② MD5

③ SHA-1

④ AES

> **정답: ④**
> MD4, MD5, SHA-1은 해시 함수(단방향 암호화), AES는 대칭키 블록 암호화 알고리즘이다.
