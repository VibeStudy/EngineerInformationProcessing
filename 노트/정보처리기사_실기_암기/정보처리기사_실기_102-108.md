

## 102 SQL-DDL (18)

Q. 다음 \<student> 테이블을 참고하여 'name' 속성으로 'idx_name'이라는 인덱스를 생성하는 SQL문을 작성하시오 

\<student>

|stdid|name|score|deptid| 
|--|--|--|--|
|2001|brown|85|PE01|

A: CREATE INDEX idx_name ON student(name);

---

Q. 아래의 <요구사항>을 만족하는 테이블 \<patient>를 정의하는 SQL 문을 작성하시오 

```
• id(문자 5), name(문자 10), sex(문자 1), phone(문자 20)을 속성으로 가진다.

• id 속성은 기본키이다

• sex 속성은 f 혹은 m만 사용한다. 제약조건명은 sex_ck 

• id는 <doctor> 테이블에 있는 doc_id를 참조한다. 제약조건명은 id_fk
```

A: CREATE TABLE patient(
    id CHAR(5) PRIMARY KEY,
    name CHAR(10),
    sex CHAR(1),
    phone CHAR(20),
    CONSTRAINT sex_ck CHECK(sex IN ('f', 'm')),
    CONSTRAINT id_fk FOREIGN KEY(id) REFERENCES doctor(doc_id)
);

---

Q. 아래의 <요구사항>을 만족하는 테이블 \<Instructor>를 정의하는 SQL문을 작성하시오 

```
• id(문자 5), name(문자 15), dept(문자 15) 속성을 가진다. 

• id 속성은 기본키이다 

• name 속성은 Null이 올 수 없다 

• dept 속성은 <Department> 테이블의 'dept' 속성을 참조하는 외래키이다 

• <Department> 테이블에서 튜플이 삭제되면 관련된 모든 튜플의 'dept' 속성의 값은 NULL로 변경되어야 한다 

• <Department> 테이블의 'dept' 속성이 변경되면 <Instructor> 테이블의 관련된 모든 속성 값도 같은 값으로 변경되어야 한다. 
```

A: CREATE TABLE Instructor(
    id CHAR(5) PRIMARY KEY,
    name CHAR(15) NOT NULL,
    dept CHAR(15),
    FOREIGN KEY(dept) REFERENCES Department(dept)
        ON DELETE SET NULL
        ON UPDATE CASCADE
);

---

Q. \<patient> 테이블에 데이터 타입이 문자 20자리인 'job' 속성을 추가하는 SQL문을 작성하시오 

A: ALTER TABLE patient ADD job CHAR(20);

---

Q. 아래의 <요구사항>을 만족하는 뷰 \<CC>를 정의하는 SQL 문을 작성하시오 

```
• <Course>와 <Instructor> 릴레이션을 이용한다 

• <Course>의 'instructor' 속성 값과 <Instructor>의 'id' 속성이 같은 자료에 대한 view를 정의한다 

• <cc> 뷰는 'ccid', 'ccname', 'instname' 속성을 가진다 

• <cc> 뷰는 <Course> 테이블의 'id', 'name', <Instructor> 테이블의 'name' 속성을 사용한다.
```

A: CREATE VIEW CC(ccid, ccname, instname)
AS SELECT Course.id, Course.name, Instructor.name
FROM Course, Instructor
WHERE Course.instructor = Instructor.id;

---

Q. <사원> 테이블을 정의하는 SQL문이다. 아래의 <요구사항>을 만족하도록 괄호(①, ②)에 적합한 명령을 넣어 SQL문을 완성하시오.


```
<요구사항> 

• '근무지번호'는 <근무지> 테이블의 '근무지번호'를 참조하는 외래키이다. 

• <근무지>테이블에서 '근무지번호'가 삭제되면 <사원> 테이블의 '근무지번호'도 삭제된다 

```

```
<SQL> 

CREATE TABLE 사원
    ( 
    사원번호 NUMBER(4) PRIMARY KEY, 
    사원명 VARCHAR(10), 
    근무지번호 NUMBER(2) ( ① ) 근무지,
    ON DELETE ( ② )
    ); 
```

A: ① REFERENCES ② CASCADE

---

Q. \<STUDENT> 테이블의 ssn 속성에 대해, 중복을 허용하지 않도록 'Stud_idx'라는 이름으로 오름차순 인덱스를 정의하는 SQL문을 작성하시오.

A: CREATE UNIQUE INDEX Stud_idx ON STUDENT(ssn ASC);

---

Q. 다음은 기본키인 '직위' 속성의 값으로 '사원', '대리', '과장', '부장', '이사', '사장'만을 허용하고, 기본 값으로 '사원'을 취하는 무결성 제약 조건을 설정하기 위한 SQL문이다. 괄호(①, ②, ③)를 채워 SQL문을 완성하시오 

```
CREATE DOMAIN 직위 VARCHAR(10)
( ① )
( ② ) VALID-직위 ( ③ );
```
A: ① DEFAULT '사원' ② CONSTRAINT ③ CHECK(VALUE IN('사원', '대리', '과장', '부장', '이사', '사장'))

---

Q. <직원> 테이블에 대해 '이름' 속성으로 '직원_name'이라는 인덱스를 정의하는 SQL문을 작성하시오.

A: CREATE INDEX 직원_name ON 직원(이름);

---

Q. 다음 <처리 조건>에 부합하는 SQL문이 완성되도록 괄호에 적합한 옵션을 쓰시오.

```
<처리 조건> 

• <학생> 테이블을 제거한다 
• <학생> 테이블을 참조하는 모든 데이터도 함께 제거한다. 
```

```
<SQL문> 
DROP TABLE 학생 (  ); 
```

A: CASCADE

---

Q. 데이터 언어는 데이터베이스를 구축하고 이용하기 위한 데이터베이스 관리 시스템과의 통신 수단이다. 데이터 언어는 데이터베이스의 구조를 정의 및 변경하는 DDL, 데이터를 조작하는 DML, 보안 및 무결성, 병행 제어 등을 위한 DCL로 구분된다. 다음 <보기>에서 DDL에 속하는 명령어들을 골라 적으시오 

```
<보기> 

•Commit •Table •Insert •Domain 
•Update •Query •Create •Trigger 
•Alter •Rollback •Procedure •Drop 
•Select •Grant •Delete •Revoke •View
```

A: CREATE, ALTER, DROP

---

Q. 다음 <사원> 테이블의 구조를 참고하며 미완성된 SQL문을 완성하시오.

<사원> 

|직원 코드|성명|직책|연봉|
|-|-|-|-|
|161353|김미나|대리|2300|
|181323|최영락|사원|1900|
|151453|홍진호|과장|2800|
|135485|구준표|과장|3000|
|104895|강나래|팀장|3600|
|165484|김하늘|대리|2400|

```
<SQL>

CREATE TABLE 사원 
    (
    직원코드 NUMBER NOT NULL, 
    성명 CHAR(10) UNIQUE, 
    직책 CHAR(10)( ① )(직책 ( ② )('사원', '대리', '과장', '팀장')), 
    연봉 NUMBER
    );
```

A: ① CHECK ② IN 

---

Q. \<BOOK> 테이블의 title 속성에 대해, 중복을 허용하지 않도록 'book_title_idx'라는 이름으로 내림차순 인덱스를 정의하는 SQL문을 작성하시오.

A: CREATE UNIQUE INDEX book_title_idx ON BOOK(title DESC);

---

Q. 아래의 <요구사항>을 만족하는 테이블 \<reservation>을 정의하는 SQL 문을 작성하시오.

```
• r_id(문자 5), user_id(문자 10), room_no(숫자 3), status(문자 10)을 속성으로 가진다.

• r_id 속성은 기본키이다.

• status 속성은 '예약', '취소', '완료'만 사용할 수 있다. 제약조건명은 status_ck이다.

• user_id는 <member> 테이블의 id를 참조한다. 제약조건명은 user_fk이다.
```

A: CREATE TABLE reservation(
    r_id CHAR(5) PRIMARY KEY,
    user_id CHAR(10),
    room_no NUMBER(3),
    status CHAR(10),
    CONSTRAINT status_ck CHECK(status IN ('예약', '취소', '완료')),
    CONSTRAINT user_fk FOREIGN KEY(user_id) REFERENCES member(id)
);

---

Q. \<product> 테이블의 price 속성을 숫자 8자리로 변경하는 SQL문을 작성하시오.

A: ALTER TABLE product MODIFY price NUMBER(8);

---

Q. 다음 <처리 조건>에 부합하는 SQL문이 완성되도록 괄호에 적합한 옵션을 쓰시오.

```
<처리 조건>

• <부서> 테이블을 제거한다.
• <부서> 테이블을 참조하는 데이터가 있으면 테이블을 제거하지 않는다.
```

```
<SQL문>
DROP TABLE 부서 (  );
```

A: RESTRICT

---

Q. 아래의 <요구사항>을 만족하는 뷰 \<EMP_DEPT>를 정의하는 SQL 문을 작성하시오.

```
• <Employee>와 <Department> 릴레이션을 이용한다.

• <Employee>의 'dept_id' 속성 값과 <Department>의 'id' 속성이 같은 자료에 대한 view를 정의한다.

• <EMP_DEPT> 뷰는 'emp_id', 'emp_name', 'dept_name' 속성을 가진다.

• <EMP_DEPT> 뷰는 <Employee> 테이블의 'id', 'name', <Department> 테이블의 'name' 속성을 사용한다.
```

A: CREATE VIEW EMP_DEPT(emp_id, emp_name, dept_name)
AS SELECT Employee.id, Employee.name, Department.name
FROM Employee, Department
WHERE Employee.dept_id = Department.id;

---

Q. 다음은 성별 속성의 값으로 'M', 'F'만을 허용하고, 기본 값으로 'M'을 취하는 무결성 제약 조건을 설정하기 위한 SQL문이다. 괄호(①, ②, ③)를 채워 SQL문을 완성하시오.

```
CREATE DOMAIN 성별 CHAR(1)
( ① )
( ② ) VALID-성별 ( ③ );
```

A: ① DEFAULT 'M' ② CONSTRAINT ③ CHECK(VALUE IN('M', 'F'))

## 103 SQL-DCL (12)

Q. 데이터를 제어하는 DCL의 하나인 ROLLBACK에 대해 간략히 서술하시오 

A: 데이터베이스 조작 작업이 비정상적으로 종료되었을 때, 변경된 내용을 취소하고 이전 상태로 되돌리는 명령어이다.

---

Q. 데이터를 제어하는 DCL의 하나인 GRANT 기능에 대해 간략히 서술하시오.

A: 데이터베이스 사용자에게 사용 권한을 부여하는 명령어이다.

---

Q. DBA가 사용자 PARK에게 테이블 [STUDENT]의 데이터를 갱신할 수 있는 시스템 권한을 부여하는 SQL문을 작성하려고 한다. 다음 SQL문을 완성하시오.

```
( ① ) UPDATE ( ② ) STUDENT TO PARK;
```

A: ① GRANT ② ON

---

Q. DCL을 이용하여 다음 요구 사항에 맞는 SQL문을 작성하시오. (Q1~Q4). 

```
<학사관리 시스템 스키마> 

학생(__학번__, 주민번호, 이름, 학년, 전화번호, 주소)
강좌(__강좌번호__, 강좌명, 학점, 수강인원, 강의실, 학기, 연도, 교수번호) 
수강(__학번__, __강좌번호__, 성적) 
교수(__교수번호__, 주민등록번호, 이름, 직위, 임용년도) 
```

Q1. 김하늘에게 <학생> 테이블에 대한 접근 및 조작에 관한 모든 권한을 부여하는 SQL문을 작성하시오.

A1: GRANT ALL ON 학생 TO 김하늘;

Q2. 김하늘에게 <강좌> 테이블에 대해 삭제하는 권한을 부여하고, <강좌> 테이블에 대해 삭제하는 권한을 다른 사람에게 부여할 수 있는 권한을 부여하는 SQL문을 작성하시오.

A2: GRANT DELETE ON 강좌 TO 김하늘 WITH GRANT OPTION;

Q3. 임꺽정에게 부여된 <교수> 테이블에 대한 SELECT, INSERT, DELETE 권한을 취소하는 SQL 문을 작성하시오.

A3: REVOKE SELECT, INSERT, DELETE ON 교수 FROM 임꺽정;

Q4. <수강> 테이블에 대해 임꺽정에게 부여된 UPDATE 권한과 임꺽정이 다른 사람에게 UPDATE 권한을 부여할 수 있는 권한, 그리고 임꺽정이 다른 사람에게 부여한 UPDATE 권한도 모두 취소하는 SQL 문을 작성하시오.

A4: REVOKE UPDATE ON 수강 FROM 임꺽정 CASCADE;

---

Q. 트랜잭션은 데이터베이스에서 하나의 논리적 기능을 수행하기 위한 일련의 연산 집합으로서 작업의 단위가 된다. 트랜잭션 연산 COMMIT의 개념을 서술하시오.

A: 트랜잭션 처리가 정상적으로 완료되었음을 선언하고, 수행 결과를 데이터베이스에 영구적으로 반영하는 명령어이다.

---

Q. <학생> 테이블에 대해 다음 SQL문을 순차적으로 수행하였으나 작업 중 문제가 생겨 <학생> 테이블의 상태를 학번이 '2001'과 '2002'인 학생의 정보가 남아있는 상태로 복원하려고 한다. 적합한 ROLLBACK 명령을 작성하시오.

<학생>

|학번|이름|학과|
|--|---|---|
|2001|한서현|생명공학|
|2002|김영현|인공지능|
|2003|명호준|유전공학|
|2004|유연우|컴퓨터공학|

\<SQL문>

```SQL
DELETE FROM 학생 WHERE 학번 = 2004;
COMMIT;
DELETE FROM 학생 WHERE 학번 = 2003;
SAVEPOINT P1; 
DELETE FROM 학생 WHERE 학번 = 2002;
SAVEPOINT P2;
DELETE FROM 학생 WHERE 학번 = 2001;
```

복원하려는 <학생> 테이블 상태 

|학번|이름|학과|
|--|---|---|
|2001|한서현|생명공학|
|2002|김영현|인공지능|

A: ROLLBACK TO P1;

---

Q. 데이터를 제어하는 DCL의 하나인 SAVEPOINT에 대해 간략히 서술하시오.

A: 트랜잭션을 수행하는 중간 지점을 지정하여, 이후 ROLLBACK 시 지정한 지점까지 되돌릴 수 있게 하는 명령어이다.

---

Q. 다음 SQL문은 사용자 MIN에게 <BOOK> 테이블의 데이터를 조회할 수 있는 권한을 부여하는 SQL문이다. 괄호(①, ②)에 들어갈 알맞은 명령어를 쓰시오.

```
( ① ) SELECT ( ② ) BOOK TO MIN;
```

A: ① GRANT ② ON

---

Q. 사용자 KIM에게 부여된 <ORDER> 테이블에 대한 INSERT 권한을 취소하는 SQL문을 작성하시오.

A: REVOKE INSERT ON ORDER FROM KIM;

---

Q. 사용자 LEE에게 <MEMBER> 테이블에 대한 SELECT 권한을 부여하고, 해당 권한을 다른 사용자에게 부여할 수 있는 권한도 함께 부여하는 SQL문을 작성하시오.

A: GRANT SELECT ON MEMBER TO LEE WITH GRANT OPTION;

---

Q. 다음 설명에 해당하는 트랜잭션 제어 명령어를 쓰시오.

```
트랜잭션 처리 중 변경된 내용을 모두 취소하고, 가장 최근의 COMMIT 상태로 되돌린다.
```

A: ROLLBACK

---

Q. 사용자 PARK에게 부여된 <SALES> 테이블의 DELETE 권한을 취소하되, PARK가 다른 사용자에게 부여한 DELETE 권한은 함께 취소하지 않는 SQL문을 작성하시오.

A: REVOKE DELETE ON SALES FROM PARK RESTRICT;

## 104 SQL-DML 

## 105 DML-SELECT 1

## 106 DML-SELECT 2 

## 107 DML-JOIN 
